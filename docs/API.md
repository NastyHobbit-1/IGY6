# Runtime API

The Rust gateway serves the public API.

## Health Checks
- GET /health/live
- GET /health/ready

Default ports:
- Web UI: 13000
- API: 18000

Application routes are served by the Rust gateway. Use the web UI for safe, authorized operations.