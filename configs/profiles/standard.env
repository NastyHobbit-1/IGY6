# IGY6 Standard profile (single-user, explicit approvals)
EXTERNAL_MODEL_POLICY_DEFAULT=blocked
APPROVAL_REQUIRED_DEFAULT=true
SINGLE_USER_MODE=true
NEXT_TELEMETRY_DISABLED=1
# LLM left off by default (enable in Settings or with Ollama auto-detect)

